language: ruby

rvm:
  - 1.9.3
  - 2.0.0
  - 2.1.8
  - 2.2.4
  - 2.3.0
  - rbx-2
  - ruby-head

bundler_args: '--without debug'

cache: bundler

sudo: false

matrix:
  allow_failures:
    - rvm: 1.9.3
    - rvm: rbx-2
    - rvm: ruby-head

script: bundle exec rake
